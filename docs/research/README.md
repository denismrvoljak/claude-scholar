# docs/research/

Research logs from `/research` (`research_<topic>_<date>.md`) and verification
reports from `/verify-sources` (`verification_<description>_<date>.md`).

These record the exact queries run and what was and was not found. Literature-heavy
work often has to report its search protocol in the methodology chapter, and
reconstructing it months later is guesswork.
