# figures/

Self-contained HTML figures built by `/figure`, numbered consecutively.

In most institutions a figure pasted as an image costs a fixed number of characters
against the length limit regardless of its size, while a table typed as text costs
its actual characters. Check `docs/requirements.md` before deciding which form a
visual should take — the rule often means a small table is cheaper as prose.
